# This file is modified from the auto-generated version from the Firebase CLI
# https://github.com/firebase/firebase-tools

name: PR Deploy Preview to Firebase Hosting
on: pull_request

jobs:
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: macos-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1
        # with:
        #   ref: master

      - name: Setup Node
        uses: actions/setup-node@v2
        with:
          node-version: '14.7.0'
          cache: 'yarn'

      # - name: Install Prince
      #   run: |
      #     sudo apt-get install gdebi-core
      #     sudo curl https://www.princexml.com/download/prince_14.2-1_ubuntu20.04_amd64.deb -o prince_14.2-1_ubuntu20.04_amd64.deb
      #     sudo gdebi prince_14.2-1_ubuntu20.04_amd64.deb

      - name: Install Prince
        run: |
          /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
          brew install prince

      - name: Install and Build 🔧
        run: |
          yarn install
          yarn build:all

      - name: Deploy 🚀
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_CODEOFMUSIC_16A81 }}'
          projectId: codeofmusic-16a81
