name: Notion2Github
on:
  pull_request:
  push:
    branches:
      - master
jobs:
  auto-sync-from-notion-to-github:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Notion2Github
        uses: younho9/notion2github@v1.1.0
        with:
          database-url: 'https://www.notion.so/Chapters-7fdcf02a4fd54b5cae266a2c4df26ff5'
          docs-directory: markdowns
        env:
          NOTION_TOKEN: ${{ secrets.TOKEN_NOTION }}

      - name: Format documents
        uses: creyD/prettier_action@v3.1
        with:
          prettier_options: --write ./markdowns/**/*.md
          commit_message: 'notion: Update docs (auto)'
        env:
          GITHUB_TOKEN: ${{ secrets.TOKEN_GITHUB }}